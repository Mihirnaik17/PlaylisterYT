name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: Test & build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: |
            server/package-lock.json
            client/package-lock.json

      - name: Install server dependencies
        working-directory: server
        run: npm ci

      - name: Run server unit tests
        working-directory: server
        run: npm run test:ci

      - name: Install client dependencies
        working-directory: client
        run: npm ci

      # Legacy CRA/Jest suite — kept for backward compatibility.
      - name: Run client unit tests (Jest / react-scripts)
        working-directory: client
        run: npm run test:ci

      # Modern Vitest suite — component & behaviour tests.
      # This is the suite you'll grow with new feature tests.
      - name: Run client unit tests (Vitest)
        working-directory: client
        run: npm run test:vitest

      - name: Build client
        working-directory: client
        env:
          REACT_APP_API_URL: http://localhost:4000
        run: npm run build
